/**
 * @recruiting-agent/core
 * 
 * Core types and schemas for the recruiting pipeline
 */

export type {
  UserProfile,
  EmailMessageDraft,
  CompanyReviewItem,
  StreamEvent,
  InterruptChunk,
} from "./types";

export { UserProfileSchema } from "./schemas";
